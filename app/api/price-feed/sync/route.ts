import { NextResponse } from "next/server";import { verifyInventoryAccess } from "@/lib/firebase-admin";import { apiError } from "@/lib/inventory/api";import { syncPriceFeed } from "@/lib/inventory/jobs";export async function POST(request:Request){try{const actor=await verifyInventoryAccess(request,["admin","procurement_manager"]);return NextResponse.json(await syncPriceFeed(actor.uid));}catch(e){return apiError(e)}}
