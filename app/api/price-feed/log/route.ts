import { NextResponse } from "next/server";import { verifyInventoryAccess } from "@/lib/firebase-admin";import { firestoreClient } from "@/lib/firestore";import { apiError } from "@/lib/inventory/api";import { serialize } from "@/lib/inventory/service";export async function GET(request:Request){try{await verifyInventoryAccess(request,["admin","procurement_manager","finance_analyst"]);const s=await firestoreClient().collection("online_price_feed_log").orderBy("created_at","desc").limit(500).get();return NextResponse.json({feedLog:s.docs.map(serialize)});}catch(e){return apiError(e)}}
